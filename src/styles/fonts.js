import metrics from './metrics';

const fonts = {
  bigger: metrics.screenHeight * 0.06,
  regular: metrics.screenHeight * 0.04,
  medium: metrics.screenHeight * 0.03,
  small: metrics.screenHeight * 0.02,
  tiny: metrics.screenHeight * 0.015,
};

export default fonts;
